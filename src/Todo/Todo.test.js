import React from 'react';
import ReactDOM from 'react-dom';
import Todo from './Todo';

describe('Todo ', () => {
//  it('should render', () => {
//    const wrapper = shallow(<Todo />);
//    expect(wrapper).toMatchSnapshot({ id: expect.any(Number), date: expect.any(Date)});
//  })
});


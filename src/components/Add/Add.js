import React from 'react';
import PropTypes from 'prop-types';
import './Add.scss';

const Add = ({...props}) => (
	<div className="add">
		+
	</div>
)

Add.defaultProps = {};

Add.propTypes = {};

export default Add;
